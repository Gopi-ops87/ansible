- name: installing packages using loops
  hosts: local
  connection: local
  tasks:
  - name: install multiple packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip